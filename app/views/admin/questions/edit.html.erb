<h1><%= question_form_title(@question) %></h1>

<%= render 'form', question: @question %>

<p>
  <%= link_to 'Show', admin_question_path(@question) %> |
  <%= link_to 'Back', admin_test_path(@question.test) %>
</p>
